Bundled ExifTool for Linux builds (fetched automatically by npm run fetch-exiftool).

Expected layout:
  exiftool          Perl launcher script
  lib/              ExifTool Perl libraries
  .exiftool-version Version pin matching scripts/exiftool-version.txt
