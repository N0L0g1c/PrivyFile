Bundled ExifTool for Windows builds (fetched automatically by npm run fetch-exiftool).

Expected layout:
  exiftool.exe
  exiftool_files/   (entire directory from the official zip)
  .exiftool-version Version pin matching scripts/exiftool-version.txt

Download source: SourceForge exiftool-{version}_64.zip
